{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit *)",
            "command": "cd \"$(git rev-parse --show-toplevel)\" && make check >&2 || jq -nc '{hookSpecificOutput:{hookEventName:\"PreToolUse\",permissionDecision:\"deny\",permissionDecisionReason:\"make check failed — fix lint/format/test issues before committing.\"}}'",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
