# Dev tooling for the runner — installed via `make install-dev`.
# Runtime deps (httpx, pyyaml) install into the same runner/.venv on demand.

httpx>=0.27
pyyaml>=6.0

pytest>=8.0
pytest-cov>=5.0
black>=24.0
isort>=8.0.1
flake8>=7.0
mypy>=1.11
types-PyYAML>=6.0
pip-audit>=2.7
